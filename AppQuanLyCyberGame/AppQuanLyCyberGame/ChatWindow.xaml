<Window x:Class="AppQuanLyCyberGame.ChatWindow"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        DataContext="{StaticResource CVM}"
        Title="Chat" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListBox ItemsSource="{Binding Chat}" Grid.Row="0">
            <!-- Define how each item in the ListBox will be displayed -->
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <!-- Assuming you want to display the message content -->
                    <TextBlock Text="{Binding Mess}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBox Text="{Binding CurrentMessage, Mode=TwoWay}" Width="400" Margin="5" />
            <Button Content="Send" Command="{Binding SendMessageCommand}" Margin="5" />
        </StackPanel>
    </Grid>
</Window>